#include <stdio.h>

/**
 * main - entry
 * for - checks and does at the same time
 *
 * Return: should be zero
 */
int main(void)
{
  int ch, vertch;
  for(ch = '0' ; ch <= '9' ; ch++)
    {
      putchar (ch);
    }
      for(vertch = ch ; vertch <= '9' ; vertch++)
	{
	  if (ch !=)
	putchar (ch);
        putchar (vertch);
        }      
      putchar('\n');
  return (0);
}
