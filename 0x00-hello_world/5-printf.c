#include <stdio.h>

/**
 * main - sets it up
 * printf - prints input
 * Return: returns the outcome
 */
int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,/n");

	return (0);
}
