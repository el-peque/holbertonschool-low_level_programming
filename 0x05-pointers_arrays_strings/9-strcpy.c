#include "main.h"
#include <string.h>

/**
 * _strcpy - copies the string pointed
 * @dest: char dest
 * @src: char src
 */

char *_strcpy(char *dest, char *src)
{
	strcpy(dest, src);
	return (dest);
}
