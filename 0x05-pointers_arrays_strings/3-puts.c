#include "main.h"

/**
 * _puts - prints a string, followed by a new line, to stdout
 * @*str: char *str
 */

int _puts(char *str)
{
	int i;

	for(
	_putchar(str[i]
	long int n[i] = *s;

	return(n);
}
