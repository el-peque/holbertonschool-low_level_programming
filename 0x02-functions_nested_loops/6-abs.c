#include "main.h"

/**
 * _abs - computes the absolute value of an integrer
 * @nem: input character
 *
 * Return: 1 if its lowercase, 0 otherwise
 */
int _abs(int n)
{
	if (n > 0)
	{
		return (int);
	}
	else if (n < 0)
	{
		return (int**(- 1));
	}
	else
	{
		return (0);
	}
}
