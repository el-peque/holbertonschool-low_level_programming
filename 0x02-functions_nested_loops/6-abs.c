#include "main.h"

/**
 * _abs - computes the absolute value of an integrer
 * @nem: input character
 *
 * Return: 1 if its lowercase, 0 otherwise
 */
int _abs(int)
{
	int;

	if (int > 0)
	{
		return (int);
	}
	else if (int < 0)
	{
		return (int**(-1));
	}
	else
	{
		return (0);
	}
}
