#include "main.h"

/**
 * rot13 - encodes a string using rot13
 * @a: char a
 *
 * Return: not sure yet
 */

char *infinite_add(char *n1, char *n2, char *r, int size_r)
{
	r = n1 + n2;
	return (r);
}
